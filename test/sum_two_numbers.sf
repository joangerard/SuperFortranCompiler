BEGINPROG SumTwoNumbers
/* It contains invalid rules like the program name has special characters, some variables starts with numbers, etc. */
VARIABLES kiloloppk, nick, number, a123, result    // single comment
  READ(number)              // Read a number from user input
  READ(a123)              // Read a number from user input
  result := number + a123
  result := 2
  result := 100
  result := 6
  result := -30
  PRINT(result)

ENDPROG